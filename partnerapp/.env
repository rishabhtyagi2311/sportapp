


BACKEND_URL ="http://10.0.2.2:5000/api"

# Must be prefixed with EXPO_PUBLIC_ or Expo will not inline it into the client bundle.
# Use your PC's LAN IP (not 10.0.2.2 - that alias only resolves on the Android emulator)
# when testing on a physical device over Wi-Fi. Update this if your PC's IP changes.
# Temporarily pointed at the live Render backend to test via Expo Go on a
# physical device — swap back to the LAN IP above when developing against
# your local server again.
EXPO_PUBLIC_API_BASE_URL="https://sportapp-nbvc.onrender.com/api/v1"
